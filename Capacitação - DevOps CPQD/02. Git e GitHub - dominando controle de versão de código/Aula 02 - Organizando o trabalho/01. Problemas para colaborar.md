https://visualizegit.com/

https://git-school.github.io/visualizing-git/

![[Pasted image 20260623140602.png]]

![[ChatGPT Image 23 de jun. de 2026, 14_10_26.png]]

---------------------------
O comando:

```
git checkout -b nova_funcionalidade
```

faz **duas coisas ao mesmo tempo**:

1. **Cria uma nova branch** chamada `nova_funcionalidade`
2. **Muda você para essa branch automaticamente**

Ou seja, ele é o atalho para estes dois comandos:

```
git branch nova_funcionalidadegit checkout nova_funcionalidade
```

-----------------------------------

## Resumo bem direto

| Comando                             | O que faz                                     |
| ----------------------------------- | --------------------------------------------- |
| `git switch main`                   | Vai para a branch `main`                      |
| `git switch nome`                   | Vai para a branch `nome`                      |
| `git switch -c nome`                | Cria a branch `nome` e entra nela             |
| `git switch -C nome`                | Força criar/resetar a branch e entra nela     |
| `git switch -`                      | Volta para a branch anterior                  |
| `git switch --track origin/nome`    | Cria branch local rastreando a remota         |
| `git switch --detach hash`          | Vai para um commit sem criar branch           |
| `git switch --discard-changes nome` | Troca de branch descartando alterações locais |
| `git switch -m nome`                | Troca tentando mesclar alterações locais      |
| `git switch --orphan nome`          | Cria branch sem histórico anterior            |

|Comando|O que faz|Entra na branch?|
|---|---|---|
|`git branch -m master nova_funcionalidade`|**Renomeia** a branch `master` para `nova_funcionalidade`|Não necessariamente|
|`git branch nova_funcionalidade`|**Cria** uma nova branch chamada `nova_funcionalidade`|Não|
|`git switch -c nova_funcionalidade`|**Cria** a branch `nova_funcionalidade` e já entra nela|Sim|

